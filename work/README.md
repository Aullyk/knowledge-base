# Work scopes

One folder per work workspace, named after the workspace itself
(e.g. `work/acme/` for a workspace called `acme`), each with `people/`,
`projects/` and `knowledge/` inside. Records under `work/<workspace>/`
are only visible to tasks running in that workspace.
